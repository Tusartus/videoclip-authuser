import { EventBlocker.DirectiveDirective } from './event-blocker.directive';

describe('EventBlocker.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EventBlocker.DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
